<Window x:Class="RideSharing.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RideSharing.App"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <materialDesign:Card Padding="20px">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80px"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <materialDesign:Card Margin="5px">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtFrom" Margin="10px" Height="30px"></TextBox>
                        <TextBox Grid.Column="1" x:Name="TxtTo" Margin="10px" Height="30px"></TextBox>
                        <Button Grid.Column="2" Margin="10px" x:Name="BtnSubmit" Click="BtnSubmit_OnClick"
                                IsDefault="True">Submit</Button>
                    </Grid>
                </materialDesign:Card>
                <wv2:WebView2 Margin="5px"
                    Grid.Row="1" x:Name="webview"
                              Source="https://ondrej-mahdalik.github.io/ridesharing-cdn/EmbedMap/" />
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
